var express = require('express');

/* GET house data. */
const house = function(req, res, next) {
  // TODO lookup address by postcode
  // TODO /house should return address
  // TODO /house should return JSON
  res.send('info');
};

module.exports = house;
